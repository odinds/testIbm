/**
 * Configuration angular application
 */
var app = angular.module('proofFabrica', ['ui.router']);

/*
app.config(function($locationProvider) {
	  $locationProvider.html5Mode({
	      enabled: true,
	      requireBase: false
	    }).hashPrefix('!');
	});
*/